<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-9">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 px-3 py-2 mb-5 inline-block rounded-md text-sm"  href='/logout'>Logout</a>
        </div>
    
       <h5 class="mb-4 text-3xl ">Update Post</h5>
       <form action="/update/<%=post._id%>" method="post">
        <textarea placeholder="what's on your mind?"class="p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id="" ><%= post.content %></textarea>
        <input type="Submit" class="px-3 w-43 text-sm py-2 bg-yellow-500 rounded-md block" value="Update Post">
       </form>
            </div>
        </div>
    </div>
</body>
</html>