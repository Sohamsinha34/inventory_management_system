<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-9">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 px-3 py-2 mb-5 inline-block rounded-md text-sm"  href='/logout'>Logout</a>
        </div>
        <h3 class="text-3xl mb-3"><span class="font-light text-4xl">Profile of Mr.</span><%= user.name %></h3>
       <h5 class="mb-4">we have to create a new post</h5>
       <form action="/post" method="post">
        <textarea placeholder="what's on your mind?"class="p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id="" ></textarea>
        <input type="Submit" class="px-3 w-43 text-sm py-2 bg-blue-500 rounded-md block" value="create new post">
       </form>
        <div class="post mt-20">
            <h3 class="text-sm text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
               <% user.posts.reverse().forEach(function(post){ %>  
                <div class="post w-1/3 mb-2 p-3 border-2 border-zinc-800 rounded-md">
                    <h4 class="text-blue-500 text-sm mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> likes</small> <!--length denote the count of like pressed-->
                    <div class="btns flex mt-5 gap-3">
                        <a class="text-blue-500" href="/like/<%=post._id %>"><%=post.likes.indexOf(user._id)=== -1?"Like":"Unlike" %></a>
                        <a class="text-yellow-500" href="/edit/<%=post._id %>">.edit</a>
                    </div>
                </div>
              <%  }) %>
            </div>
        </div>
    </div>
</body>
</html>